# 项目状态与下一步

最后更新：2026-08-07

---

## 1. 当前状态：调研目标已达成，工具已完工

本项目的立项目的是回答「这批 `.pic` 数据能不能解析、怎么解析」。**已经回答完了。**

| 项 | 状态 |
|---|---|
| SDFF 格式逆向 | ✅ 完成，82/82 文件解析成功，无未知类型码 |
| 属性树解析器 | ✅ 纯 stdlib，24 个测试 |
| 位号抽取 + 就近标签匹配 | ✅ 4,708 条绑定 |
| 工程点表读取 | ✅ 50,877 位号，含描述/量程/单位/报警限 |
| 流程图 ↔ 点表 join | ✅ 命中率 98.3%（未命中的是表达式而非位号） |
| `access_parser` Jet4 解码 bug | ✅ 已定位并绕过，28.27% → 0 |

**解析器侧没有已知缺陷，也没有待办**。后续如果不推进下面的 P0，这个仓库可以就此
停在当前状态——它已经是一个自洽、可复用的交付物。

## 2. 下一步

> **2026-08-07 重排**。上一版把「拿到配对时序数据」列为 P0，
> 依据是「手上没有配对时序数据」——**该判断已被推翻**：配对数据存在，
> 见 [research-notes.md §9](research-notes.md)。原 P0 因此降级，排序整体前移。

### P0 — 跑 218 个 `LogicPic`，导出控制逻辑接线（零开发成本）

工程 zip 的 `Control/CA0/CS<NN>/LogicPic/` 下有 218 个 `.pic`，是**控制逻辑图**。
已实测：**同一 SDFF 格式，本解析器零改动就能读**，`Text`/`Tag` 流直接给出
功能块类型与**引脚级接线**（`X.BKOUT -> Y.BKIN` 这种）。见
[research-notes.md §6.4](research-notes.md)。

这是目前投入产出比最高的一件事——不写代码，只是把现有 CLI 多跑一个目录。
产出是**控制因果**（谁的输出接谁的输入、联锁怎么传播），
比几何推导出来的管道连通关系有用得多。

先要确认的一件事：218 个文件在各控制站的分布，以及覆盖哪些回路。

### P1 — 把点表字典与位号台账导成产物存档（低成本，建议尽早做）

```bash
sdff tags <流程图目录> -o tags.csv --project <解压后的 SUPCON_PROJECT>
```

工具跑一次就有，但**源数据（那份工程备份 zip）将来未必还在手上**。
趁现在能读，先把 50,877 条字典固化成 CSV/JSON 存档。

> 导出产物含真实位号，属于客户数据，**不要提交进本仓库**（`.gitignore` 已拦 `*.csv`）。

### P2 — 与该厂时序库做一次 join 验证（原 P0，已降级为可执行任务）

配对数据存在，位号交集 96.1%（[§9.2](research-notes.md)）。
剩下的是**接入**而不是获取：连上时序库，抽样验证位号 → measurement 的对应关系，
以及点表报警限与现场运行值是否同步——即 [§7](research-notes.md) 结尾列的那两个待确认项。

这一步做完，§7 的价值才从「调研结论」变成「已验证结果」。

### P3 — 解析 `.fbd` 功能块图（唯一可能拿到整定参数的路径）

工程 zip 里有 **95 个 `.fbd`**（功能块图）没碰过。

排到 P3 而不是更前，是因为已经核对过：**点表里没有 PID 整定参数**
（`FB` 表无参数列，`RefParam` 表为空），`.fbd` 是包内唯一还有可能存参数值的地方。
但它**不是 SDFF**（魔数 `24 00 02 00`），要另做一次完整逆向——
成本是 P0 的量级之上，且不保证里面真有参数。

**建议先走 P0**：LogicPic 已经能白拿到接线关系，
`.fbd` 只在「确实需要参数值且业务侧要不到」时才立项。

### 暂不建议做

| 项 | 理由 |
|---|---|
| 管道连接拓扑推导 | 几何推拓扑工作量大；且 P0 的逻辑图直接给控制因果，比几何连通更有用，这条路更不划算了 |
| 发布到 PyPI | 没有外部使用者之前意义不大 |
| 加 CI | 单人维护 + 测试 0.03s 跑完，收益低于维护成本 |
| `clsid` → 图元类型名映射 | 位号抽取用不上，纯粹的完整性追求 |

## 3. 仓库约定

### 不收任何客户工程数据

`.pic` / `.mdb` / `.csv` / `.zip` 已在 `.gitignore` 中排除。测试用**合成的 SDFF 文件**
做往返验证（`tests/conftest.py` 按格式规格反向生成字节），不依赖任何现场数据。

### 文档里的位号与描述都是虚构样例

本仓库是公开的。所有文档与测试中的位号（`P1_PT100101A01_PV` 之类）、中文描述、
路径均为**结构同形的虚构样例**——保留前缀/装置码/回路号/后缀的形态，只换具体编号，
这样格式说明依然成立而不外泄现场信息。

**格式规格、解码逻辑、实测数量级统计是真实的。**

往仓库里加内容时守住这条：不要贴真实位号、DCS 口令、客户工程名或本地数据路径。
改 Jet4 解码示例时注意 hex 与字符串要同步改并复验自洽。

### 文档不点名厂商，但不假装匿名

包名、CLI、仓库名、文档正文一律按**格式**（`SDFF`）称呼，不用厂商名与产品名。
理由：逆向文件格式做互操作是正当的，但没必要让一个公开仓库读起来像针对某一家公司。

**保留的例外是数据里的字面标识**——工程目录名 `SUPCON_PROJECT/`、
`.ssf` 的 `SUPCON FILE IN…` 魔数等。删掉它们规格就是错的、工具也没法用。
所以这套做法降低的是"显眼程度"，不是"可追溯性"；任何人拿这些串一搜就知道是谁家的
格式。**这是有意的取舍，别误以为仓库是匿名的。**（背景见
[research-notes.md §2](research-notes.md)）

### 历史起点：`ffcc124`

首个 commit 曾包含未脱敏内容。因为「后来删掉」并不能让它从 git 历史里消失，
公开前用孤儿分支重建了历史，**脱敏前的版本已不存在**。
不要去找更早的 commit，也不要从别处恢复它。

⚠️ 但 `f4fd575`（`docs: 补记会话结论…`）里写进过一处真实时序库路径与三个真实测点名，
**且已经 push 到公开远程**。后续 commit 已改成通用表述，
**但公开历史里的那一版删不掉了**。

**2026-08-07 决定：接受，不清除。** 内容是一个内网 IoTDB 库名 + 3 个测点名，
推不出更多信息，泄露面小于 `filter-repo` + force push 打断已建立历史的代价。
**这条已经拍板，不要再重新讨论**；除非出现新的合规要求，那时的动作是
`filter-repo` 重写 + force push，或把远程转私有。

## 4. 开发环境备注

解析器本身只用 stdlib，任何 Python ≥3.10 都能跑。读点表需要 `access_parser`：

```bash
pip install -e ".[tagdb,dev]"
pytest
```

> 注意别用不带 pip 的精简 venv 跑安装——本机默认解释器可能就是这种，
> 装依赖与跑测试请显式指定一个完整的 Python（如 conda 环境的解释器）。
